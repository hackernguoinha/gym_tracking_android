<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GymTracer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GymTracer" parent="Base.Theme.GymTracer" />

    <style name="Widget.MyApp.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="android:background">?attr/colorSurface</item>
        <item name="itemIconTint">@color/selector_nav_color</item>
        <item name="itemTextColor">@color/selector_nav_color</item>
    </style>
    <style name="TopLineIndicator">
        <item name="android:color">@android:color/transparent</item>
        <item name="android:shape">@null</item>
        <item name="android:gravity">top</item>
    </style>
</resources>